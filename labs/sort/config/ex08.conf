id=sort-08
title=Exercise 8: Combined Options
description=Use multiple sort options together: sort by the second field numerically in reverse order. Data format is ID Name Position.
lab_dir=labs/sort/ex08
validator=sort_combined
target_file=output.txt
hint=sort -k 1 -rn data.txt > output.txt

