id=sort-01
title=Exercise 1: Check if Sorted
description=Verify if data.txt is already sorted. The file is NOT sorted, so sort it first to output.txt then verify. Finally, redirect to output.txt.
lab_dir=labs/sort/ex01
validator=sort_check
target_file=output.txt
hint=sort data.txt > output.txt && sort -c output.txt
completed=0
